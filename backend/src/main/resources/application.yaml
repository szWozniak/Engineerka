spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/dronhub
    username: dronhub
    password: zaq12wsx
    driver-class-name: org.postgresql.Driver
  jpa:
   hibernate:
      ddl-auto: update
   properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: true
sharedfolder:
  path: "/shared_directory/data.csv"
  interval: 2000
